@model Onestop.Layouts.Settings.TemplatedItemPartSettings
    <fieldset>
        @Html.EditorFor(m => m.AllowTemplateChoice, new {@class="check-box"})
        <label for="@Html.FieldIdFor(m => m.AllowTemplateChoice)" class="forcheckbox">@T("Allow template choice")</label>
    </fieldset>